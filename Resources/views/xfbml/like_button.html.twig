<fb:like href="{{ url }}" layout="{{ options.layout }}" action="{{ options.action }}" width="{{ options.width }}" show_faces="{{ options.show_faces ? 'true' : 'false' }}" share="{{ options.share ? 'true' : 'false' }}" colorscheme="{{ options.colorscheme }}" kid_directed_site="{{ options.kid_directed_site ? 'true' : 'false' }}"></fb:like>