<fb:like-box href="{{ url }}" width="{{ options.width }}" height="{{ options.height }}" colorscheme="{{ options.colorscheme }}" show_faces="{{ options.show_faces ? 'true' : 'false' }}" header="{{ options.header ? 'true' : 'false' }}" stream="{{ options.stream ? 'true' : 'false' }}" show_border="{{ options.show_border ? 'true' : 'false' }}" force_wall="{{ options.force_wall ? 'true' : 'false' }}"></fb:like-box>