<div class="fb-like-box" data-href="{{ url }}" data-width="{{ options.width }}" data-height="{{ options.height }}" data-colorscheme="{{ options.colorscheme }}" data-show-faces="{{ options.show_faces ? 'true' : 'false' }}" data-header="{{ options.header ? 'true' : 'false' }}" data-stream="{{ options.stream ? 'true' : 'false' }}" data-show-border="{{ options.show_border ? 'true' : 'false' }}" data-force-wall="{{ options.force_wall ? 'true' : 'false' }}"></div>